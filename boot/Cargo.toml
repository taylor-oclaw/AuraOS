[package]
name = "aura-boot"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = "0.11"

[build-dependencies]

# This binary builds the bootable disk image
[[bin]]
name = "mkimage"
path = "src/main.rs"
